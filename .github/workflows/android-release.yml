name: Build APK (Phone-only installable)

on:
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install deps + build web (ROOT)
        shell: bash
        run: |
          set -e
          npm install

          # لو فيه build script شغّله، غير كده جرّب vite build
          if npm run | grep -qE ' build'; then
            npm run build
          elif [ -f "vite.config.js" ] || [ -f "vite.config.ts" ]; then
            npx vite build
          else
            echo "No build script found and no Vite config. Skipping web build."
          fi

      - name: Add Android platform + sync Capacitor (ROOT)
        shell: bash
        run: |
          set -e
          npx cap add android || true
          npx cap sync android

      - name: Build DEBUG APK (installable)
        shell: bash
        run: |
          set -e
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
